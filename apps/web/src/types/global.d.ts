// Augment the Window interface with app-global properties here.
// Empty by default — extend per-feature as you go.
declare global {
  interface Window {
    // example:
    // analytics?: { track: (event: string, props?: Record<string, unknown>) => void };
  }
}

export {};
