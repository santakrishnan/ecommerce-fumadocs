// Public surface of the shared backend client. Uncomment when the client is
// activated; features import `apiClient` from `@shared/services/api`.
// export { apiClient } from "./client";

export {};
