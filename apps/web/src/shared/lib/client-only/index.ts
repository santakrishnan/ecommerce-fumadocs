"use client";

/**
 * Client-only utilities — browser APIs (IndexedDB, React hooks).
 * Only import from "use client" modules.
 */
export {
  type IdbCollectionConfig,
  type IdbRecord,
  idbCollectionOptions,
} from "../idb-collection/idb-collection-options";
