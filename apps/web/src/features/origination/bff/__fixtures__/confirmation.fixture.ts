/**
 * Static copy for the confirmation screen — the full-size interstitial shown
 * when the user reaches a progress milestone in the origination flow.
 *
 * Kept as a fixture so the message lives in one place, separate from the
 * rendering component. Variants (different messages per milestone) will be
 * layered on top of this later.
 */

export const CONFIRMATION_MESSAGE_DEFAULT = "Your phone number has been verified";

export const CONFIRMATION_MESSAGE_RED = "Your personalized financing offer is ready!";
