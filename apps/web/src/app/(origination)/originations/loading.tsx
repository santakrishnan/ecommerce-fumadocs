import { OriginationEntryLoading } from "@features/origination";

/**
 * /originations loading UI — the flow's initial loading screen, shown by the
 * App Router while the page resolves, before the first step renders.
 */
export default function OriginationLoading() {
  return <OriginationEntryLoading />;
}
