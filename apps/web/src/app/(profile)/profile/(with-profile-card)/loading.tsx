import { ProfilePageSkeleton } from "./profile-page-skeleton";

/**
 * Full-page loading skeleton for the /profile route.
 *
 * Mirrors the T1 page structure with Figma-matched heights
 * and 32px (gap-8) spacing between sections.
 */
export default function ProfileLoading() {
  return <ProfilePageSkeleton />;
}
