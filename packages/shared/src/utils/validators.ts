/**
 * Re-exported from the canonical `utils` package.
 * Do not duplicate — import from here or directly from `utils/validators`.
 */
export { isValidEmail, isValidPhone, isValidURL } from "utils/validators";
