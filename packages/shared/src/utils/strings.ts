/**
 * Re-exported from the canonical `utils` package.
 * Do not duplicate — import from here or directly from `utils/strings`.
 */
export { capitalize, formatSlugLabel, toCamelCase, toKebabCase } from "utils/strings";
